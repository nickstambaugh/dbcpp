#include "query_parser.h"

QueryParser::QueryParser() {
    //todo
}

std::string QueryParser::parse(const std::string& query) {
    //return default for now
    std::string parsedQuery = "Parsed query: " + query;
    return parsedQuery;
}
